{
  "name": "@econmind/calibration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "c4:feature-evidence:generate": "pnpm build && node dist/feature-admission-cli.js && pnpm exec prettier --write ../../data/calibration/preflight/c4_feature_admission_evidence.v1.json",
    "c4:gate-diagnostics:generate": "pnpm build && node dist/gate-diagnostics-cli.js && pnpm exec prettier --write ../../data/calibration/preflight/c4_gate_diagnostics.v1.json",
    "c4:preflight:verify": "pnpm build && node dist/preflight-cli.js",
    "exploration:generate": "pnpm build && node dist/exploration-cli.js && pnpm exec prettier --write ../../data/calibration/exploration/*.json",
    "pilot:live": "pnpm build && node dist/pilot.js && pnpm exec prettier --write ../../data/calibration/pilot/*.json",
    "v10:data-handoff:generate": "pnpm build && node dist/v10-readiness-cli.js && pnpm exec prettier --write ../../data/calibration/preflight/v10_data_handoff_readiness.v1.json",
    "typecheck": "tsc -p tsconfig.json --pretty false"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
