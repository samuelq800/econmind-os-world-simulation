# V10.4 browser E2E target preparation

**State:** `PREPARATION_ONLY_NOT_GATE_B_BROWSER_E2E`
**Immutable base:** `ebc5fcbe010559d5d6a94e86792a77ecc02502d2`
**Branch:** `codex/f-gate-b-browser-e2e-preparation`

## Bound scope

V10.4 calls for real two-country/two-Office browser evidence and an independent
Gate B decision. The current World Web entry `index.html` is a public preview;
`command.html` and `prototype.html` render typed local fixtures, including a
local mock receipt. `apps/world-api/src/runtime.ts` serves health/readiness,
not browser command or authorized projection routes. Thus those pages cannot
establish the requested real E2E today.

Build a short-lived local Chrome smoke target under `tests/e2e/`. It launches
the built Vite preview on loopback, uses a fresh temporary headless browser
profile, verifies the rendered preview disclaimers, and emits an explicit
`NOT_RUN` matrix for real identity, read, command/receipt and reconnect cases.
Its output must never be named Gate B PASS. The harness must refuse a
non-loopback target and must not use any production credentials.

## Ownership and authority

Only new `tests/e2e/` script/manifest/readme and preparation evidence may be
edited. D owns World Web pages; A/C own Core; E owns staging. No API, DB,
migration, production or main-site changes. The smoke script reads local built
HTML/DOM and readiness only. It writes no World State, command, event, receipt,
outbox or cache. Effective scope is P2 non-authoritative test tooling; Gate B
remains a separate P0 hard review.

## Checks and exit

Run the pinned web build, local headless Chrome preflight if available, focused
manifest checks, scoped lint/format and diff check. Record browser version,
exact implementation SHA, local-only target, real observations and every
`NOT_RUN` gap. Do not update status/Gate or merge this candidate.
