# Execution plan for V02.1

## Authority and scope

- Sources: Constitution environment, RLS, and production rules; ADR-18; V01 integration contract.
- Goal: fail-closed local/CI database isolation and production fingerprint rejection.
- Exclusions: production access, business schema, seed data, RLS mutation.

## Gate and risk

- Dependencies: V00.3 and V01.3 are `VERIFIED`; V01 is integrated.
- Effective risk: P0, production-environment boundary.
- Continuation: feature-branch implementation only; independent review is deferred to Gate A by the explicit sprint instruction, never treated as verification.

## Change and validation

- Extend canonical environment assessment without logging secrets.
- Test missing, invalid, conflicting, cross-environment, production-like URL, and CI-secret cases.
- Run targeted environment, boundary, secret, and governance checks.
