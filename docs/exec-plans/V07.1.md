# V07.1 planning — Command/Event Schema 与不可变 Ledger

## Planning status and authority

- Exact R2 title: `Command/Event Schema 与不可变 Ledger`.
- Work package: V07 — `命令、事件、回执与确定性重放基础` (Stage S1).
- Execution mode: `NORMAL`.
- Current status: `PLANNED`; this document is planning only and does not move
  V07.1 to `IN_PROGRESS`.
- Authoritative step sources: `planning/r2_steps.json`,
  `planning/R2_33_WORK_PACKAGES_101_STEPS.md`, `prompts/steps/V07.1.md`,
  `PLANS.md`, and the Constitution/Master source units cited below.
- Governing integration and World Core sources:
  `docs/architecture/V01_TWO_REPOSITORY_INTEGRATION_CONTRACT.md`,
  `docs/architecture/WORLD_CORE_CONTRACT.md`,
  `docs/architecture/WORLD_CORE_JIT_ADR_PACK.md`,
  `docs/architecture/WORLD_CORE_OWNER_ADR_DECISION_PACK.md`, and
  `docs/planning/V06_V10_IMPLEMENTATION_SEQUENCE.md`.

No runtime, schema, migration, status, or evidence record is changed by this
planning task. Recommendations in an unapproved ADR are planning inputs only;
they are not approved architecture.

## Hard dependencies and entry gate

The exact V07.1 hard dependencies are:

1. V02.3 — `VERIFIED`.
2. V03.3 — `VERIFIED`.
3. V05.3 — `VERIFIED`.
4. V06.3 — `VERIFIED` through the approved V06 package review and owner-authorized
   mainline closure.

The following owner-required chain is complete and repository state has been
recomputed from the resulting mainline:

```text
V06_PACKAGE_APPROVED
-> OWNER_ACCEPTANCE
-> V06 promotion/closure
-> merge to main
-> dependency recomputation
```

That chain satisfies the step dependencies but is not sufficient to start
V07.1. Before schema freeze or implementation begins, the responsible human
owner must also approve:

- ADR-11, `幂等重复回执与不可变 event 消费状态`; and
- the V07 core boundary of ADR-17, `持久化、提交、重放与故障边界`.

Both remain `PROPOSED_NOT_APPROVED`. The ADR dependency map names V07.1 as their
first affected step and says they block affected implementation by their V07
gate. ADR-20 is not a V07.1 implementation gate: its exact JIT point is before
V07.2 permits queued execution. V07.1 therefore must not invent queued
authorization or final receipt behavior. ADR-16 is required before any
candidate DDL is merged or promoted; it is not authority to create schema or
migration files during this planning-only task.

At implementation entry, re-read `status/progress.json`, `status/decisions.json`,
the exact main SHA, all dependency evidence, and all owner approval records.
Chat history and this plan are not substitutes for that recomputation.

## Purpose and acceptance gate

Exact R2 purpose:

> 建立 command_id、event_id、payload hash、simulation/real timestamps、append-only event store。

Exact R2 acceptance gate:

> 同ID异payload拒绝；历史 event 不 UPDATE/DELETE。

The smallest implementation satisfying that gate establishes canonical,
versioned Command and Event envelopes; stable identity and payload
fingerprinting; distinct simulation and real-audit timestamps; and an ordered,
append-only event persistence boundary. It does not implement economic engines
or the later receipt, outbox, replay executor, lease, or transaction packages.

## Source-law anchors

- Constitution U0206 and U0646: the same initial state, commands, events, and
  seed must produce the same output.
- Constitution U0209-U0214 and U0648-U0650: scheduled work is idempotent; every
  Command and Event has a unique ID; repeated network requests and processing
  state must be auditable.
- Constitution U0217-U0220 and U0652-U0653: one economic fact is recorded once;
  correction is a new Event and never a rewrite of an old ledger Event.
- Constitution U0471-U0475: UI writes use authoritative Commands; snapshots are
  accelerators and cannot replace ledger/current-table authority; performance
  cannot weaken atomicity, RLS, idempotency, reconciliation, or Source of Truth.
- Master U0079-U0081: `ImmutableEventLedger` is the append-only history within
  the single World State; correction uses reversal/correction Events.
- Master U0193-U0196 and U0205-U0223: E01 owns scheduled identity/order and
  idempotency keys; duplicate delivery/payment cannot execute twice.
- Master U1918: the broad event audit vocabulary is a source requirement, but
  final V07.1 field names and nullability remain subject to approved ADR-11/17
  and schema review.

Source-unit registration is traceability, not an implementation claim.

## Ownership boundaries

### Command ownership

- `packages/core` owns the pure, canonical Command envelope, branded Command ID,
  schema/version registry contract, and deterministic fingerprint preimage.
- `apps/world-api` is the authentication and Command/query boundary. It may
  validate and persist an immutable submission through the controlled intake
  path, but it may not execute economic mutations or assign Event order.
- External `AuthSubject` remains a canonical UUID distinct from resolved World
  `ActorId`, `CountryId`, and `OfficeId`. Claimed routing values never replace
  server-side resolution.
- `apps/world-web`, the main site, V1/Legacy, caches, projections, snapshots,
  replay views, and forecasts cannot own or directly mutate a Command outcome.

Subject to ADR-11 approval, the fingerprint must cover every authoritative
intent field and exclude transport/real-audit metadata. It must use the repaired
V03 inert canonical serialization and fixed SHA-256 preimage convention. The
same Command ID or idempotency identity with different authoritative intent
must fail with zero authoritative mutation.

### Event ownership

- `packages/core` owns the pure Event envelope and versioned Event-type/schema
  contracts.
- `apps/world-worker` is the authoritative execution host and is the only
  component allowed to assign per-World Event sequence/order inside the later
  controlled commit path. Clients never provide ordering.
- Event history is append-only. UPDATE, DELETE, resequencing, or overwriting a
  correction is forbidden; correction/reversal is a new Event.
- Operational errors and delivery status are not economic ledger Events.

The planned minimum Event envelope preserves `eventId`, `worldId`, per-World
sequence, `WorldVersion`, causation Command identity, correlation identity,
SimTime, Event type, schema version, and canonical payload. This is a planning
shape from the current World Core contract, not a schema freeze while ADR-17 is
unapproved.

### Receipt semantics and scope split

V07.1 establishes only the identities and immutable references that later
receipts require. V07.2 owns accepted-versus-executed semantics, immutable
acceptance acknowledgement/final receipt, durable duplicate-return behavior,
consumer receipts, outbox, queue/claim, and delivery deduplication. Accordingly:

- V07.1 may reject same-ID/different-payload at its boundary.
- V07.1 must not claim that the final receipt/outbox lifecycle is implemented.
- Event `processed_at` or consumer state must not be implemented by updating a
  historical Event; V07.2 will use a separate consumer receipt/delivery record.
- ADR-20 must be approved before V07.2 queues a Command; no acceptance-time
  context becomes execution authority in V07.1.

### Replay requirements and scope split

V07.1 must preserve replay-complete immutable facts: stable sequence,
WorldVersion transition, causation, SimTime, type/schema version, canonical
payload, and verifiable canonical bytes/hash material. It must fail closed on
unsupported versions and structurally corrupt ordering rather than creating a
best-effort history.

V07.3 owns the replay executor, opening-seed provenance, reducer registry,
seeded RNG interface, engine/model/registry version binding, contiguous replay,
and live-versus-replayed hash comparison. Snapshots/checkpoints remain
rebuildable accelerators and never become a second Source of Truth.

### Authoritative writer boundary

The only state-change path remains:

```text
authorized command
-> validated receipt/idempotency
-> deterministic worker
-> atomic append-only events/postings
-> derived projections
```

For V07.1, this means the API can own immutable intake and the Worker alone can
assign authoritative Event order. The durable per-World lease, fencing token,
stale-writer rejection, private candidate, WorldVersion recheck, and atomic
economic/Event/receipt/outbox commit are V09.1-V09.2 responsibilities. V07.1
must leave enforceable interfaces for those later guarantees without claiming
them as implemented.

## Dependency on V06 SimTime and scheduler

- Command/Event SimTime fields must use V06's branded, non-negative integer
  simulation-millisecond ticks; real timestamps are audit/operational metadata
  only and cannot determine an economic deadline or outcome.
- The scheduler's approved total order remains
  `(dueSimTime, priorityRank, scheduledEventId)`. V07.1 does not replace it with
  network arrival, database row order, object iteration, or wall-clock order.
- A scheduled identity completed by V06 can causally produce ordered ledger
  Events, but `ScheduledEventId` and economic `EventId` remain distinct types.
- V06's command cutoff is the immutable SimTime observed when the Worker begins
  the authoritative transaction. V07.1 records compatible identities/time
  values; V09 owns that transaction.
- PAUSED contributes zero SimTime and RUNNING downtime uses deterministic
  catch-up. Ledger replay must consume the recorded canonical advancement and
  scheduling outcomes, never recompute them from ambient wall time.

## Deterministic and idempotent requirements

1. Equivalent inert canonical envelopes produce identical fingerprint bytes;
   field insertion order, transport metadata, and server audit time cannot
   change economic identity.
2. Getter, method/provider, Proxy, hidden/symbol state, sparse array,
   unsupported prototype/container, JS-number exact value, and unknown schema
   versions fail closed before behavior or mutation.
3. `commandId`, `eventId`, schema/type IDs, SimTime, and WorldVersion remain
   branded/canonical; `AuthSubject` cannot be substituted with a domain ID.
4. Same ID with different authoritative payload/fingerprint is a deterministic
   conflict with no new Event or WorldVersion transition.
5. Event sequence is unique and contiguous per World at the controlled writer
   boundary; insertion/network order cannot select it.
6. Historical Event bytes are immutable. Correction adds a causally linked
   Event and never edits or deletes the original.
7. In-memory duplicate tracking, last-write-wins, payload-only comparison, or a
   client-supplied order is never authoritative.

Exact same-payload retry returning the stored acknowledgement/final receipt and
never re-executing is the target proposed by ADR-11 and required by the V07
package gate, but its durable end-to-end implementation belongs to V07.2/V09.
It must not be reported as delivered by V07.1 alone.

## Expected implementation files

These are the expected future touch points after all entry gates pass. Exact
paths must be revalidated against updated `main`; no listed file is authorized
for modification by this planning task.

| Area                     | Expected file or path                                                           | Planned responsibility                                                                                                                |
| ------------------------ | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Core identity            | `packages/core/src/ids.ts`                                                      | Add distinct branded Command/Event identities without weakening existing IDs.                                                         |
| Command contract         | `packages/core/src/commands/**`                                                 | Canonical immutable envelope, schema validation, fingerprint input/result, conflict result types.                                     |
| Event contract           | `packages/core/src/events/**`                                                   | Canonical immutable envelope, Event type/schema registry, sequence and causation contracts.                                           |
| Public core surface      | `packages/core/src/index.ts`                                                    | Export only reviewed canonical contracts.                                                                                             |
| API intake               | `apps/world-api/src/**`                                                         | Thin validated immutable Command-submission boundary; no economic executor.                                                           |
| Worker ledger adapter    | `apps/world-worker/src/**`                                                      | Worker-only Event append/order interface; no V09 lease/commit claim.                                                                  |
| Persistence candidate    | `database/migrations/artifacts/<next>_world_v2_command_event_ledger.sql`        | Future World head/Command/Event tables and append-only/sequence constraints, subject to ADR-16/17 and immutable migration provenance. |
| Migration authority      | `database/migrations/manifest.json`                                             | Future ordered ID/hash/source-commit record; no second migration chain or direct production publication.                              |
| Examples                 | `tests/world-core/command-event-contracts.test.ts`                              | Envelope, version, identity, conflict, ordering, and append-only examples.                                                            |
| Properties               | `tests/property/command-event-properties.test.ts`                               | Canonical equivalence, attack values, ID/payload mutation, and ordering properties with recorded seed/path.                           |
| Persistence/architecture | `tests/world-core/command-event-ledger.test.ts`, `tests/architecture/**`        | Disposable-database constraint checks and API/Worker/UI ownership negatives.                                                          |
| Evidence                 | `docs/reports/V07.1/IMPLEMENTATION.md`, `docs/reports/V07.1/TEST_EVIDENCE.json` | Honest immutable candidate, commands/results, failures, gaps, ADRs, and production-mutation `NONE`.                                   |

Do not create a parallel event store, migration manifest, state owner, receipt
store, or replay truth to avoid an unresolved decision.

## Test matrix

| Test surface             | Required cases                                                                                                                       | Required oracle/evidence                                                            | Gate                              |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | --------------------------------- |
| Command schema           | valid envelope; missing/extra field; malformed ID/UUID; unknown command/schema version; exact-domain violation                       | deterministic accept/reject; zero mutation on reject                                | P0                                |
| Canonical fingerprint    | property-order permutations; equivalent exact values; audit/transport changes; each authoritative intent field changed independently | equal intent gives identical bytes/hash; changed intent changes fingerprint         | P0                                |
| Behavioral serialization | getters, arbitrary methods/providers, Proxy, hidden/symbol state, sparse arrays, unsupported prototypes/containers                   | reject before any trap/getter/method observation                                    | P0                                |
| ID/payload conflict      | same Command ID with changed key, actor, scope, type, version, payload, or expected WorldVersion                                     | explicit conflict; no Event/head/version delta                                      | P0                                |
| Command/Event separation | forged Event ID/order from API or client; Command ID reused as Event ID                                                              | distinct branded types and server-owned ordering reject misuse                      | P0                                |
| Ledger append            | append ordered Events; duplicate ID/sequence; gap/resequence; UPDATE/DELETE; correction                                              | unique ordered append succeeds; all history mutation fails; correction is new Event | P0                                |
| Time boundary            | V06 SimTime values at zero/day/year/cutoff/catch-up; real-audit timestamp changes                                                    | canonical SimTime preserved; real time cannot alter fingerprint/outcome/order       | P0                                |
| Version/replay readiness | unsupported Event type/schema; missing/duplicate/out-of-order sequence; corrupted canonical bytes/hash                               | explicit stop at offending input; no skip or silent repair                          | P0                                |
| Writer boundary          | UI/API direct authoritative Event write; worker-assigned sequence contract; projection/snapshot write-back                           | forbidden writers denied; no second authority                                       | P0                                |
| Migration provenance     | next artifact ID/order/hash; full commit/path/exact-byte proof; clean and existing isolated rehearsal; UPDATE/DELETE denial          | existing V02 validator/rehearsal passes; production mutation `NONE`                 | P0, only after ADR-16/17 approval |
| Regression               | V03 exact serialization/types, V05 identity/authorization, V06 clock/order, architecture scanners, environment safety                | no Foundation/V06 regression and no forbidden import/coercion path                  | P0/P1                             |
| Tooling/performance      | formatter, lint, typecheck, focused/full tests, stable registry lookup and bounded envelope size                                     | all actual commands/results recorded; skipped/NOT_RUN stays explicit                | P1                                |

Property evidence records the exact fast-check seed, path, run count, candidate
commit, and schema/registry versions. A green unit substitute cannot satisfy
real PostgreSQL append-only/constraint evidence when that surface is claimed.

## P0 risks

- Freezing ADR-11 duplicate/fingerprint/receipt semantics or ADR-17 canonical
  persistence/writer/replay semantics without owner approval.
- A second Source of Truth, direct UI/API economic write, or client-selected
  Event order.
- Same identity with different intent being accepted, overwritten, or treated
  as a retry; or duplicate delivery creating a second economic fact.
- Mutable ledger history, consumer state stored by updating Events, silent
  correction, non-contiguous/resequenced Event history, or replay corruption
  being skipped.
- Mixing wall time with SimTime, losing the V06 scheduler order/cutoff, or
  conflating ScheduledEvent and economic Event identities.
- Behavioral/non-canonical fingerprint input, JS-number exact values, unknown
  versions, or hash ambiguity.
- Candidate DDL without ADR-16/17 approval, V02 manifest provenance, isolated
  rehearsal, worker-only authority, or production-safety evidence.
- Claiming atomicity, single-writer enforcement, durable exactly-once effects,
  receipts, outbox, or replay before V07.2/V07.3/V09 supplies them.

Any P0 failure stops V07.1 and requires correction plus the policy-required
independent review of an immutable candidate. The implementer cannot self-mark
the step `VERIFIED`.

## P1 risks

- Public API ergonomics or registry layout accidentally freezing future Command
  families beyond the minimum versioned contract.
- Payload/envelope growth or index choices creating avoidable storage and
  lookup cost without changing correctness.
- Error names, module layout, or exported helper surface coupling V07.1 to
  future E02-E18 behavior.
- Test fixtures or generated declarations drifting from the canonical source
  contract while runtime behavior remains unchanged.

P1 issues may follow only the repository's explicit review policy; they cannot
be used to downgrade a change that also touches a P0 boundary.

## Scope exclusions

- No V07 runtime code, database schema, migration, RLS/grant, status promotion,
  implementation report, test-evidence claim, or production mutation now.
- No V07.2 acceptance/final receipt lifecycle, consumer receipt, outbox,
  queue/claim, notification retry, or durable dedupe implementation.
- No V07.3 replay executor, opening seed, reducer registry, seeded RNG, or
  engine/model/registry replay binding implementation.
- No V09 lease renewal, fencing, stale-writer rejection, private candidate,
  WorldVersion transaction, atomic commit, crash recovery, or outbox delivery.
- No inventory/financial posting (V08), projection/vertical slice (V10), or
  E02-E18 economic behavior.
- No decision on ADR-11, ADR-16, ADR-17, or ADR-20; no inferred owner approval.
- No direct shared-production Supabase publication and no alternate migration
  history, dashboard SQL authority, or Legacy/V1 table reuse as World V2 truth.

## Implementation exit and stop point

When implementation is separately authorized, V07.1 may leave
`IMPLEMENTED_UNVERIFIED` only after its exact acceptance gate, P0 test surfaces,
immutable evidence, decision records, and independent review satisfy the
central policy. It does not authorize V07.2. Until then, the authoritative state
remains `V07.1 = PLANNED`.
