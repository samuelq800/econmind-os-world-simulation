# V01.2 execution plan — ADR-01 至 ADR-20 协调与阻塞图

## Authority and scope

- Step: V01.2, ADR-01 through ADR-20 coordination and blocking graph.
- Dependency: V01.1 is `VERIFIED` by P2 owner fast-track at implementation
  commit `2d66872278b1c8a586c751390fed94796f00c40a`.
- Sources: `status/decisions.json`, the two ADR coordination documents,
  `planning/r2_steps.json`, and `planning/work_packages.json`.
- Effective risk: `P2` governance documentation/tooling.
- Highest affected boundary: no decision or P0 algorithm is changed; the graph
  only exposes future decision gates.
- Review path: explicit owner fast-track after evidence; package review after
  V01.3.

## Deliverables

1. Generate a machine-readable ADR conflict, dependency, affected-package, and
   latest-blocking-gate map.
2. Preserve every ADR status as `PROPOSED_NOT_APPROVED` and every approval
   record as `null`.
3. Distinguish future implementation blockers from blockers at the current V01
   coordination gate.
4. Reject bulk approval, unknown packages, missing gates, or map/register drift
   in automated validation.
5. Record implementation and test evidence separately from V01.1.

## Authority and behavior declaration

- Authoritative economic owners affected: none.
- Reads: governance documents and manifests.
- Writes: generated governance map and reports only.
- Commands/events/receipts/postings: none.
- DB/RLS/identity/time: none; no production access.
- Legacy impact: none.

## Validation

- Exactly ADR-01 through ADR-20, all proposed/not approved.
- Exact affected-package parity with the decision register.
- Every ADR has a latest gate and responsible human owner.
- Graph nodes/edges refer only to known ADRs, work packages, and steps.
- No unresolved blocker applies to completing this coordination-only step.
- Full repository and governance checks.

## Stop rule

Stop on any approval mutation, missing/unknown dependency, failed mandatory
check, P0 behavior change, or blocker applying to V01.2 itself.
