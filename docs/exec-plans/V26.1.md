# V26.1 — browser forecast worker protocol preparation

**Candidate state:** `PREPARATION_ONLY_NOT_V26_1_STARTED`
**Branch:** `codex/f-v26-forecast-worker-preparation`
**Immutable base:** `c91744a6d02865829b82df2088025949de99d88d`

## Authority and gate

`prompts/steps/V26.1.md`, `planning/r2_steps.json`, the Constitution R004/R015,
`AGENTS.md`, `PLANS.md`, the repository ownership boundary and the centralized
review policy govern this slice. V03.3 and V07.3 are recorded `VERIFIED` at this
base; V10.4 remains `PLANNED`. ADR-12 is `PROPOSED_NOT_APPROVED`. This is the
explicitly permitted parallel preparation, not a V26.1 product start, approval,
or change to either record.

## Scope and owners

- Add a browser-only Comlink transport and pure request lifecycle under
  `apps/world-web/src/forecast/`. The worker receives only caller-supplied,
  already-authorized snapshot facts; it has no fetch, credential, persistence,
  command, event or authoritative-state access.
- Define a versioned snapshot identity including world, country, scope,
  authorization revision, projection, model, source snapshot and WorldVersion.
  The client discards results after any identity change or newer request.
- Provide task cancellation and deterministic unavailable/cancelled outcomes.
  The browser worker returns `MODEL_NOT_CONNECTED` until an authorized forecast
  model and classified projection are integrated in a later step.
- Tests exercise cancellation, stale completion, identity changes, missing
  inputs, invalid task identity and Comlink RPC behavior.

**Affected owner:** `apps/world-web` only. **Reads:** caller-supplied derived
snapshot and explicit assumptions. **Writes:** ephemeral worker/client memory
only. **Commands/events/receipts/postings:** none. **DB/RLS:** none.
**Environment:** browser module worker only; no Supabase access. **Time:** no
simulation clock or wall-time dependence. **Legacy impact:** none. No changes
to SixOffices, map, CSS or the original EconMind site.

## Review and validation

The highest touched boundary is non-authoritative browser presentation, a P2
preparation candidate. Authorization/classification policy and WorldVersion
authority remain external; this slice does not resolve ADR-12. Run focused
Vitest, world-web typecheck/build, ESLint, Prettier, boundary/secret checks and
`git diff --check`. Record actual exit codes and explicitly mark integration,
database, V10.4 and product acceptance `NOT_RUN`. Freeze and push one exact
candidate SHA for review; do not update status or merge.
