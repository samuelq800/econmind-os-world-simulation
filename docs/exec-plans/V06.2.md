# Execution plan for V06.2

## Authority and entry gate

- R2 title: 调度、Pause/Resume 与 Catch-up.
- Hard dependency: V06.1. Its immutable code/evidence candidates are
  `41fd476a221e7d14f5fc5fec76cafeb8d9263dc7` and
  `390367442fca12cd511e5df7199d6c1dc49c345a`; it remains
  `IMPLEMENTED_UNVERIFIED`.
- Continuation authority:
  `docs/governance/WORLD_CORE_V06_CONTINUATION_POLICY.json`, limited to
  adjacent V06 steps with package review before V07.
- Current status: `IMPLEMENTED_UNVERIFIED` at code candidate
  `767b718d995210eafc4742c1a5a93e798329598e`; Project Session A does not
  claim independent verification.

## Planned change

Add PREOPEN/RUNNING/PAUSED/ENDED transitions, scheduled-event identity and due
queue, pause intervals, exact day/year markers and running-worker catch-up.
PAUSED wall time contributes zero SimTime; resume starts from the frozen value.
All accepted numeric transitions are exact-or-reject. Future E02-E18 settlement
operations remain absent.

Authoritative owner: the single E01 clock/scheduler state. Inputs are explicit
recorded advancement, lifecycle transitions and canonical scheduled-event
records. Outputs are immutable scheduler state, exact due eligibility and
calendar-boundary markers. This step introduces no economic event execution,
Command cutoff, priority registry, settlement-stage order, database, RLS,
migration, production mutation, UI authority or player-online dependency.

## Validation and exit

Random transition sequences prove pause freeze, deterministic resume, exact due
selection, restart without duplicate execution and player-online independence.
Exit requires pause not accumulating economic time and restart not repeating a
due identity. Required evidence includes focused acceptance/unit/property
tests, realistic invalid-transition/restart paths, existing regressions,
architecture/boundary scanners, environment safety, governance validation and
the applicable full repository baseline. V06.2 ends
`IMPLEMENTED_UNVERIFIED`.
