# Execution plan for V07.3

## Authority and entry gate

- R2 title: Replay、Seed 与版本绑定.
- Hard dependency: V07.2.
- Current status: `PLANNED`.

## Planned change

Add opening-seed provenance, pure versioned reducers, contiguous replay,
canonical hash comparison and explicit seeded-RNG interface. Unknown
event/schema/model/registry versions and corrupted ledgers fail closed.

## Validation and exit

Prove live state equals replayed state byte-for-byte under the same seed and
versions. Attack missing, reordered, duplicate, tampered and unknown-version
events; no best-effort skip is allowed.
