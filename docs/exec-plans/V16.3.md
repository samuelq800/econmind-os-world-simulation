# V16.3 — Housing/Safety Emergency and Authority Invariants

## Status and boundary

`FOUNDATION_IMPLEMENTED_UNVERIFIED`; no real authorization decision or
emergency operation is performed.

Replay accepts only canonical facts with matching source/predecessor lineage,
version, snapshot and exact payload evidence.

## Scope

The pure safety calculation rejects an emergency deployment unless its inert
authority record is active and current simulation time is strictly before its
explicit expiry. Housing has no hidden subsidy, population or project shortcut.
All state-shaped outputs are caller-owned inert records with exact unit,
identifier, before/after and replay trace fields.

## Evidence target

Focused vectors reject expired emergency authority, missing/invalid commission,
fractional people/units, direct funding effects and trace-ID collisions. The
future authoritative owner must re-resolve authorization before any real action.
