# Execution plan for V09.2

## Authority and entry gate

- R2 title: 候选状态与原子 Commit.
- Hard dependency: V09.1.
- Current status: `PLANNED`.

## Planned change

Build private candidate execution and one short locked transaction that rechecks
fence/version/idempotency and re-resolves current AuthSubject, membership,
country, Office, capability, authorization revision and immutable approval
scope. It atomically stores inventory, financial postings, events, receipt,
WorldVersion and projection outbox. No cached Office context authorizes commit.

## Validation and exit

Inject failure after every economic leg and immediately before/after commit.
Pre-commit crash leaves zero change; post-commit retry returns one stored result.
Two transactions cannot both commit `N -> N+1`.
