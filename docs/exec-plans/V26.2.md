# V26.2 — scoped client cache and reconnect preparation

**State:** `PREPARATION_ONLY_NOT_V26_2_STARTED`
**Branch:** `codex/f-v26-reconnect-cache-preparation`
**Immutable base:** `f4815cc3d24e4876e9f09858f5536aac560592ef`

## Authority and gate

`prompts/steps/V26.2.md`, `planning/r2_steps.json`, Constitution R011/R014/R016
and its scoped Realtime rule, `AGENTS.md`, `PLANS.md`, and the central review
policy govern this preparation. V26.2 is a NORMAL step with hard dependencies
V07.3, V10.4, V17.3, V20.3, V23.3, V25.3 and V26.1. At this exact base,
V10.4, V17.3, V20.3, V23.3, V25.3 and V26.1 remain `PLANNED` in
`status/progress.json`; ADR-12 remains `PROPOSED_NOT_APPROVED`. The owner has
explicitly requested a reusable, isolated client preparation slice, not a
formal V26.2 implementation or status transition.

## Scope

- Add a pure browser-client cache/reconciliation module under
  `apps/world-web/src/reconnect/`. Its key binds world, AuthSubject,
  authorization revision, country, Office, scope, model, projection and
  WorldVersion. It never stores or exposes server credentials.
- Accept authoritative snapshots monotonically per scope. Reject old,
  conflicting or duplicate versions. Treat scoped outbox messages only as
  invalidation notices and retain the highest seen WorldVersion; never apply
  an economic delta from a notice.
- Mark cached projections as needing reconciliation on notice or reconnect.
  Accept a replacement only when the supplied authoritative snapshot covers
  the notice watermark; clear all cached values on authorization revocation.
- Expose explicit `UNAVAILABLE_NO_LIVE_CHANNEL` availability until a private
  channel adapter is implemented after the formal dependencies close.
- Add focused ordering, collision, scope, revocation and reconnect tests.

**Owner touched:** world-web only. **Reads:** caller-supplied authorized
snapshot or scoped notice. **Writes:** ephemeral in-memory derived cache only.
**Commands/events/receipts/postings:** none. **DB/RLS/IndexedDB:** none.
**Environment and time:** browser/Node-compatible pure TypeScript; no network,
clock or production access. **Legacy impact:** none. Do not edit D's pages,
SixOffices, map or CSS, Core, API or original EconMind website.

## Validation and exit

Effective change class is P2 non-authoritative preparation; the unresolved
authorization/classification policy is outside this module. Run focused
Vitest, web typecheck/build, scoped lint/format, architecture and secret checks,
and `git diff --check`. Record exact results and `NOT_RUN` for real private
channel, IndexedDB, classified API snapshot source, DB/RLS and product E2E.
Freeze and push a reviewable candidate; do not merge or modify status/Gate.
