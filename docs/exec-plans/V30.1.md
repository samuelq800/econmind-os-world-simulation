# V30.1 load harness — isolated preparation

Status: `PREPARATION_ONLY`; not V30.1 execution, verification, or a capacity claim.

The formal ledger still has V09.1 and the V30.1 hard dependencies as
`PLANNED`. V28.1 also has an unresolved single-World/Constitution R002 and
ADR-14 contract conflict. This slice therefore prepares only a deterministic
load schedule and a measurement summarizer. It cannot connect to a service,
send a command, generate an authoritative event, or produce a PASS decision.

## Scope and ownership

- Owner: `tools/v30` and its focused test; no product runtime module, browser
  entrypoint, migration, RLS, or original main-site file.
- Read: caller-supplied session tier, operation frequencies, seed, observations,
  and provenance metadata. All frequencies are explicit test assumptions,
  not asserted real-user behavior.
- Write: none outside returned in-memory plans and summaries. No network,
  database, Command, Event, Receipt, or World State effect.
- Time: virtual schedule seconds only; no Simulation Clock or economic deadline.
- Legacy: no route or main-site change.

## Next real V30.1 step

After V29.3 and other hard dependencies close, bind the schedule to the actual
authorized API/worker/browser routes on an approved non-production target.
Record machine, database tier, code/model versions, operation mix, failures,
P50/P95/P99, CPU/memory, and resource saturation at 50/100/420 sessions.
Do not replace measured results with the deterministic plan or stub executor.
