# Single-World Core configuration preparation (V28.1-adjacent)

## Owner correction and gate

The owner explicitly narrowed this slice to **one World only**. There is no World/Season mode split, Season-specific instance, label, time engine, participant path or economic rule in this candidate. The repository's current V28.1 step contract and ADR-14 still discuss two orchestrators; this branch does not resolve or silently rewrite that authority. The conflict belongs to later owner/governance reconciliation. V10.4, V23.3, V25.3, V26.3 and V27.3 are `PLANNED` at entry, and ADR-14 is `PROPOSED_NOT_APPROVED`, so this remains `PREPARATION_ONLY` and does not advance V28.1 status.

## Narrow implementation

Add one pure `packages/core/src/orchestration` validator for a single World configuration containing only the World ID, current shared Core model version, configured positive country count, existing Simulation Clock version, and simulation-time economic expiry discipline. Exact keys and canonical values are mandatory; no mode/discriminator, policy branch, hard-coded 70, buff, admission/registration rule, cutoff policy, economic engine or state mutation is introduced. The result is an immutable, versioned canonical snapshot and deterministic SHA-256 binding.

The only bridge takes an existing Core replay input for the same World/model and passes it unchanged to the already existing `replayAuthoritativeEvents` path. The test replays identical opening/Events twice through that shared path to demonstrate deterministic equivalence for the tested input, not a cross-mode claim or product exit gate.

## Ownership and verification

- Files: new `packages/core/src/orchestration`, focused `tests/world-core`, this plan and evidence report only. No original EconMind main-site file, existing runtime module, schema or migration.
- Reads: caller-supplied replay evidence. Writes/Commands/Events: none added. No production access, no wall-clock read, no new authorization or participation mechanism.
- Tests: strict unknown/missing-field and version rejection; configured count not hard-coded; canonical snapshot determinism; forged binding/world/model rejection; existing replay determinism; no state mutation/hidden enhancement. Run focused tests, Core build/typecheck, lint/format and boundary/safety checks. Independent review and owner contract reconciliation remain pending.
