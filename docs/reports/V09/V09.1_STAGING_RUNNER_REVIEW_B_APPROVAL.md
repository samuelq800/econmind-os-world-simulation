# V09.1 Dedicated Staging Runner — Review B Approval

## Immutable review binding

| Item                          | Exact value                                |
| ----------------------------- | ------------------------------------------ |
| Reviewed forward-fix target   | `0e79faffce3cd44001374fa31b184a594ea51328` |
| Preserved failed target       | `23dd2da39783622ce175382bd19c51968182215a` |
| Decision                      | `PASS`                                     |
| Open blockers / majors        | `0 / 0`                                    |
| Execution runner ready        | `YES`                                      |
| Actual staging evidence ready | `NO` — dedicated execution remains not run |

This independent Review B is bound only to the exact target above. It closes
the historical unrestricted-cascade cleanup blocker and the
acknowledgement-unknown/durable-evidence major for runner continuation. It does
not amend the preserved failed review, complete V09.1, authorize V09.2, promote
a migration, or authorize production/shared-target mutation.

## Focused closure evidence

- Cleanup has no `CASCADE`, database drop, `DROP OWNED`, broad reset, or
  cross-schema deletion. It validates the exact run marker, target
  fingerprint, roles, ownership, allowlisted inventory and external dependency
  boundary before the known `0006` through `0001` teardown. Every schema/object
  drop is `RESTRICT`; role drops have PostgreSQL's implicit restrictive
  semantics. Any external dependency or teardown mismatch rolls back the
  cleanup transaction and reports `CLEANUP_INCOMPLETE`.
- The acknowledgement-unknown branch obtains the primary backend's exact
  `pg_backend_pid()`, sends `COMMIT; SELECT pg_sleep(...)`, requires an
  independent recovery connection to observe that exact PID as the marked
  active `PgSleep`, terminates that PID, requires the command promise to reject,
  and then recovers the exact marker as `COMMITTED`. An unavailable observation
  or termination protocol reports `CRASH_PROTOCOL_UNAVAILABLE`.
- The pre-commit branch closes an open transaction and recovers the exact
  evidence-run marker as `PENDING`; re-acquisition at fence `1` proves rollback.
- Durable evidence is recursively redacted, written to a new `0600` temporary
  file, file-synced, atomically renamed, and parent-directory-synced. Failure is
  `DURABLE_EVIDENCE_WRITE_FAILED` after the same strict cleanup duty.
- Execution policy is evaluated before the injected client factory or
  `connect()` is invoked; the focused rejection test observes zero factory and
  connection calls.

## Independent local checks

- Node syntax checks for the policy, runner and CLI: `PASS` on Node `24.20.0`.
- Focused policy/runner Vitest: `16/16 PASS`.
- Embedded PGlite writer-lease/migration tests: `5/5 PASS`.
- Embedded clean-baseline and existing-schema migration rehearsal: `PASS`.
- Independent PGlite `RESTRICT` probe: the drop was rejected and both the
  run-owned table and external scene remained present after rollback.
- Focused ESLint and Prettier checks: `PASS`.
- Authoritative-pattern, repository-boundary and migration-manifest checks:
  `PASS`.
- Read-only R2 governance validation on an isolated exact-branch checkout:
  `PASS`; the primary review checkout remained detached at the reviewed SHA.
- GitHub `refs/heads/codex/world-core-v09` was confirmed at the reviewed SHA
  immediately before this docs-only follow-up.

No real `--execute`, remote database connection, staging mutation, production
access, full `pnpm check`, V09.1 completion, or V09.2 work occurred.

## Remaining boundary

```text
V09.1 = IN_PROGRESS
V09.2 = NOT_STARTED
execution runner = READY
dedicated non-production staging RLS/grant/crash execution = NOT_RUN
actual staging evidence = NOT_READY
main merge / production / shared staging mutation = NOT AUTHORIZED
```
