# V09.1 Review B — Changes Required

## Immutable review binding

```text
Reviewed target: 0f09b17adf3e56945fcc9e0888d2f14abe2467b1
Review result: V09_1_CODE_REVIEW = FAIL
OPEN_BLOCKER = 1
OPEN_MAJOR = 0
STAGING_EVIDENCE = PENDING
```

This report records the externally supplied focused Review B result. It is not
a Codex self-review and must remain in history even after a forward fix.

## V09.1-BLK-01 — DELETE resets fencing lineage

Review B reproduced the following against the full disposable `0001`–`0005`
schema:

1. acquire `WORKER_1` with fence `1`;
2. DELETE the `world_writer_lease` row;
3. reacquire for the same holder, which returned fence `1`; and
4. submit the old `(WORKER_1, fence=1, WorldVersion=0)` SQL guard assertion,
   which succeeded.

The original `0005` transition trigger guarded INSERT and UPDATE but did not
guard DELETE. The resulting ABA reset violates monotonic fencing and could let
a stale writer regain commit authority.

## Required forward remediation

- Preserve `0f09b17` and the original `0005` SQL/source hash unchanged.
- Add the narrowest forward migration that makes DELETE/reset incapable of
  erasing the fence lineage through a Worker-authorized mutation path.
- Add TypeScript/PGlite and real PostgreSQL coverage for rejected
  DELETE/TRUNCATE, normal expiry takeover to a higher fence, and old guard
  rejection.
- Bind new artifact provenance with the exact historical path, source commit
  and SHA-256 under `GIT_NO_REPLACE_OBJECTS=1`.
- Do not claim unavailable non-production staging RLS/grant evidence, begin
  V09.2, promote/merge the candidate, or access production.

## Current forward-fix state

`0006_world_v2_writer_lease_lineage_guard` is the candidate remediation. It
adds only DELETE/TRUNCATE rejection triggers to the existing lease table; it
does not alter `0005`, write economic facts, redefine Event/Posting semantics,
or grant database privileges. Fresh local/CI evidence and a new immutable
review target are required before Review B can be reconsidered.
