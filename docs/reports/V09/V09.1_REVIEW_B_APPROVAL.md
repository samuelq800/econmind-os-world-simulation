# V09.1 Review B — Forward Remediation Approved for Continuation

## Immutable review binding

| Item                         | Exact value                                |
| ---------------------------- | ------------------------------------------ |
| Final evidence target        | `d6819a36c8856fbc8c4da045cb8dedf87d0833d6` |
| Bound forward code candidate | `e43dd6629d8645740278156654bb224121666856` |
| Independent decision         | `APPROVED_FOR_CONTINUATION`                |
| V09.1-BLK-01                 | `CLOSED`                                   |
| Code review                  | `PASS`                                     |
| Open blockers / majors       | `0 / 0`                                    |
| Staging evidence             | `PENDING`                                  |

This record captures the externally supplied independent Review B decision. It
does not replace the immutable `0f09b17` changes-required record, promote a
migration, verify the V09 package, or authorize V09.2.

## Reviewed remediation evidence

Review B confirmed the branch-local `0006_world_v2_writer_lease_lineage_guard`
artifact from source `8ef4390beaa877aa3e3d971a4bb0587a0c7592bd`, with SHA-256
`8ee4a6b01d2916665fc9793a24bbdb9675afc216a6aaa64ae84e67cb44fa66f8` and
valid historical provenance.

The reviewed behavior is:

- lease-row DELETE and TRUNCATE are rejected;
- a normal expiry takeover yields fence `2`; and
- the former `(holder, fence=1)` database guard assertion is rejected with
  `WORLD_WRITER_FENCE_STALE`.

Review B also accepted the focused `10/10` coverage, six-migration clean and
existing-schema rehearsal, and disposable PostgreSQL CI at both the code and
final-evidence commits.

## Remaining boundary

```text
V09.1 = IN_PROGRESS
V09.2 = NOT_STARTED
0005 / 0006 = BRANCH_LOCAL_UNPROMOTED
dedicated non-production staging RLS/grant/crash evidence = NOT_RUN
main merge / production / shared staging mutation = NOT AUTHORIZED
```

## Minimum non-secret inputs for the next staging phase

1. An owner-approved, dedicated **non-production** Supabase project reference
   or endpoint, with confirmation that it is neither production nor a shared
   staging target.
2. The non-secret role names and intended ownership/grant/RLS matrix for the
   migration operator, runtime Worker, and application readers.
3. The approved disposable World/test namespace and reset/cleanup procedure.
4. The approved crash-recovery test protocol (where interruption may occur and
   how the isolated target is restored).

Any credentials must be provisioned only through the approved secret channel;
they are neither requested nor recorded in this repository.
