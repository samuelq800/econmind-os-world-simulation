# V09.1 Implementation Candidate Evidence

## Status

```text
V09.1 = IN_PROGRESS
V09.2 = NOT_STARTED
V09.3 = NOT_STARTED
V09.1 candidate migration = BRANCH_LOCAL_UNPROMOTED
V09 package review = NOT_STARTED
production access = false
production mutation = false
```

This is an implementation/evidence record, not an independent review or a
step/package verification decision.

## Superseded review state

Independent Review B evaluated immutable target
`0f09b17adf3e56945fcc9e0888d2f14abe2467b1` and returned
`V09_1_CODE_REVIEW=FAIL`, `OPEN_BLOCKER=1`, `OPEN_MAJOR=0` for
V09.1-BLK-01. The finding showed that `0005` guarded INSERT/UPDATE but allowed
DELETE of the lease row, permitting a subsequent acquisition to recreate fence
`1` and accept an old `(holder, fence=1)` assertion. The exact failed review is
preserved in `docs/reports/V09/V09.1_REVIEW_B_CHANGES_REQUIRED.md`.

`0005` is not rewritten. A new `0006` forward candidate guards DELETE/TRUNCATE
lineage reset and must receive fresh evidence and a new review target before any
continuation claim.

## Immutable candidate lineage

| Item                      | Exact value                                                            |
| ------------------------- | ---------------------------------------------------------------------- |
| Branch                    | `codex/world-core-v09`                                                 |
| V09.1 code candidate      | `23b8b7d9101a7a8fab4bd272ff0a59b58fa684d7`                             |
| CI provenance forward fix | `beed9d389b3db6a65d1f10ed6330af5a1fb05da6`                             |
| Migration ID              | `0005_world_v2_writer_lease_fencing`                                   |
| Migration source commit   | `7102cd3c3ba80cd2468032c3d4776a871dec515d`                             |
| Historical artifact path  | `database/migrations/artifacts/0005_world_v2_writer_lease_fencing.sql` |
| SHA-256                   | `1559a0a44c82f55005a0209b9b2a64095625c384e39ed0999e1a44e573ba3cb3`     |
| Manifest                  | `database/migrations/manifest.json`                                    |

The source commit is an ancestor of the candidate. `migration:validate` checks
the source commit/path bytes with `GIT_NO_REPLACE_OBJECTS=1`; a working-tree
claim cannot substitute for that provenance.

## Implemented V09.1 boundary

- `packages/core/src/writer/world-writer-lease.ts` defines inert canonical lease
  records, explicit operational-time requests, monotonic fencing acquisition,
  cutoff assertions, stale fence rejection, expiry rejection and the independent
  WorldVersion prerequisite.
- The core module receives all operational time explicitly and does not read an
  ambient clock, alter SimTime or alter replay ordering.
- `0005` creates only `world_v2.world_writer_lease`, its guarded transition
  trigger, `acquire_world_writer_lease(...)`, and
  `assert_world_writer_commit_guard(...)`. The SQL guard locks lease then World
  head and rejects stale holder/fence, expired lease and stale WorldVersion.
- The candidate does **not** add Event, Inventory Posting, Financial Posting,
  receipt, outbox, materialization, candidate economy or recovery behavior.
  Those remain exclusively V09.2/V09.3 work.

## Automated evidence

| Check                                         | Result                                                                                                                          |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| Local `pnpm check` on `beed9d3`               | PASS — 42 test files, 484 tests; 1 file / 2 tests skipped because no local real PostgreSQL target exists                        |
| Lease core + PGlite candidate tests           | PASS — 9 tests                                                                                                                  |
| Clean and existing-schema migration rehearsal | PASS — 5 manifest rows in each disposable PGlite rehearsal                                                                      |
| Migration provenance validation               | PASS — 5 migrations including `0005`                                                                                            |
| Disposable real PostgreSQL CI                 | PASS — [run 34506068925](https://github.com/samuelq800/econmind-os-world-simulation/actions/runs/34506068925), 1 file / 2 tests |

The real PostgreSQL run used two independent connections against a fresh
loopback PostgreSQL 16 service. It proved that a competing acquisition blocks
behind an uncommitted first acquisition and then fails, and that stale fence,
expiry and WorldVersion conditions are rejected inside the database guard.

## Recorded CI forward fix

The first CI attempt on `23b8b7d` failed before database testing because the
default shallow checkout omitted all historical migration source commits. The
failure was `SOURCE_COMMIT_NOT_FOUND` for `0001` through `0005` in
[run 34505913447](https://github.com/samuelq800/econmind-os-world-simulation/actions/runs/34505913447).
`beed9d3` changes only the CI checkout to `fetch-depth: 0`; the subsequent run
passes provenance validation and the real PostgreSQL test. This is a forward
CI evidence fix, not a rewrite of the SQL artifact or its source hash.

## Remaining completion boundary

ADR-18 still requires a dedicated non-production Supabase staging target for
final PostgreSQL concurrency, crash, RLS and grant evidence. That target is not
configured in this workspace and has **not** been accessed or mutated.

Consequently:

- V09.1 remains `IN_PROGRESS` rather than `IMPLEMENTED_UNVERIFIED` or
  `VERIFIED`.
- V09.2 has not begun; no economic atomic-commit implementation is claimed.
- No migration is promoted, merged to `main`, published, or applied to any
  shared/staging/production Supabase project.
- Independent V09 package review remains mandatory after all package steps and
  required evidence are complete.
