# V09.1 Staging Harness Review B — Changes Required

## Immutable review binding

```text
Reviewed target: 14692fb449f779ade6270dbee5c7c80178fb1ba8
Decision: CHANGES_REQUIRED
OPEN_BLOCKER: 1
OPEN_MAJOR: 0
ACTUAL_EVIDENCE_RUN_READY: NO
Finding: V09.1-STAGING-BLK-01
```

This externally supplied Review B result is immutable history. It does not
change the separate `d6819a3` approval of the 0006 lease-lineage remediation.

## Finding

The former policy compared WHATWG URL authority fields but did not bind them to
the effective `pg@8.16.3` connection parameters. Review B reproduced that an
approved authority plus query `host`, `port` and `user` overrides resolves to a
different pg target/identity. It also reproduced that `ssl=true&sslmode=disable`
was accepted despite pg resolving `ssl=false`.

No actual staging evidence run is ready until the URI is fail-closed under the
same parser/runtime semantics a future runner would use.

## Required forward remediation

- Preserve `14692fb` unchanged.
- Reject all ambiguous or override URI parameters affecting endpoint, identity,
  database or TLS, including duplicates and encoded nearby forms.
- Verify the effective `pg` host, port, user, database and TLS result before a
  connection could be attempted.
- Reject connection-related `PG*` environment overrides.
- Add focused local regression coverage only. Do not access a remote database,
  run unrelated full suites, change V09.2, or promote any migration.
