# V10.1 Package Audit Request

## Immutable review target

- Review code/evidence target: `e0c6d2dbca04050f428df062b58eeb796b77d4bc`
- V10.1 implementation base: `bacb8d6ac8623f0e527a63e969121a36740a78bc`
- Active branch: `codex/v10-1-implementation`
- Package status: `AUDIT_REQUESTED`; this is not a Gate B result, V10.1
  completion record, merge authorization, or production authorization.

## Candidate scope for review

The candidate supplies an HTTP-neutral, injected authenticated read handler;
server-derived Country and Office-private entitlements; Country/Office activity
projections from authoritative Events and canonical Commands; and a named-party
read source that is server-only, initially empty and cross-school-capable by
design. The party materializer requires an exact active match in current
authorization for subject, country, Office and authorization revision.

`NEGOTIATION_PARTY` has one source-bound writer. The previous generic Worker
writer has been retired so all caller-provided payload attempts reject before a
database transaction. Candidate migration 0014 contains no school field,
school lookup or school-isolation rule; it exposes no browser role grant and
there is no membership writer in this package.

## Local evidence supplied

All commands used the pinned Node `v24.20.0` and pnpm `12.3.4` toolchain.

| Check                                           | Result                                                          |
| ----------------------------------------------- | --------------------------------------------------------------- |
| World API + Worker typechecks                   | PASS                                                            |
| Focused V10.1 read/publication tests            | PASS — 6 files / 16 tests                                       |
| World read generated types                      | PASS — current                                                  |
| Migration provenance validation                 | PASS — 14 artifacts                                             |
| Migration local rehearsal                       | PASS — clean baseline and existing-schema ephemeral PGlite only |
| Architecture/boundary/authoritative scans       | PASS — 9 files / 84 tests                                       |
| Country/Office and party delete/tamper rebuilds | PASS — disposable PGlite only                                   |

The named-party generic-write Major on `8b23d3f` and its governance-test Minor
were independently closed on `88fbfe2` with an
`APPROVED_FOR_V10_1_CONTINUATION_ONLY` result. That scoped result is retained
as evidence, but it is not a package-level Gate B approval.

## Required package-audit decision

Review the exact target against `docs/exec-plans/V10.1.md`, the ADR-12
classification boundary and repository governance. In particular, determine
whether the following explicitly preserved limitations prevent the package from
moving to its required V10.1 independent-review/Gate B handoff:

- Country/Office payloads currently expose source-bound activity summaries, not
  exact ledger-derived economic summaries.
- The party membership source is intentionally empty until a separately
  authorized server workflow defines and writes actual group membership.
- There is no public route, live JWT verifier, live PostgreSQL wiring, external
  database/RLS-grant/staging proof, shared-Supabase action, merge or production
  action.

Record Blocker/Major/Minor findings with exact code evidence. An `APPROVED`
result may approve only the package handoff that the governing policy actually
permits; it must not silently convert any preserved limitation into runtime or
production evidence.
