# V10.1 Package Review

## Immutable target

| Item             | Value                                             |
| ---------------- | ------------------------------------------------- |
| Code target      | `ba46856fdcf6458156e044a4cbc6112e9e6502b5`        |
| Evidence wrapper | `b25ba34c560a0142ae017132490c01c74cce0d63`        |
| Branch           | `origin/codex/v10-1-implementation`               |
| Decision         | `V10_1_PACKAGE_APPROVED_FOR_NEXT_GOVERNANCE_ONLY` |
| Finding counts   | `BLOCKER=0`, `MAJOR=0`, `MINOR=0`                 |

Independent Review B confirmed base ancestry and that the wrapper changes only
audit/status records. The reviewed package keeps HTTP-neutral server-injected
authenticated reads, derives Country/Office and named-party projections from
authoritative Events and canonical Commands, and rejects caller-provided
generic party publishing before a transaction. Migration `0014` remains
source/provenance bound, RLS-protected, browser-role revoked, and without a
browser writer.

The B1 exact ledger-summary follow-up is included: inventory and financial
posting summaries are watermark-bounded and ledger-derived. Local evidence was
8 focused files / 29 tests, API and Worker typechecks, current generated read
types, and boundary/governance checks. No dedicated V10.1 property campaign,
live JWT/public route, real PostgreSQL, external RLS/grant proof, staging,
merge, deployment, DDL publication, or production action was performed.

This decision closes the V10.1 package-review dependency only. It permits the
next governance decision; it does not grant Gate B, merge, staging,
production, deployment, or database-migration authority.
