# V10.6 durable ledger-lineage replay increment

**Recorded:** 2026-09-14  
**Code candidate:** `0021fe0a5877739b09d8f672c8bb9dccfcb63071`

## Implemented local candidate

`DurableV08LedgerLineageReader` now performs a Worker-only, same-transaction
read of the immutable opening seed, World head, authoritative Command/Event
facts, Inventory Postings, and Financial Posting batches. It reconstructs the
same V08 ledger state solely through Core constructors and
`rebuildV08LedgersFromLineage`.

The reader fails closed when canonical payloads, hashes/fingerprints, event
order, Posting causation, or final head version/sequence do not recompute. It
accepts no browser data, cache, current balance, fixture provenance, or
caller-provided state. `WorldOpeningSeedStore.loadFrom` lets this operation
retain one server transaction snapshot without bypassing canonical seed checks.

## Local verification

- Worker typecheck and targeted ESLint — PASS.
- Durable-lineage reader, opening-seed store, and V10.4 local acceptance tests
  — PASS, 3 files / 10 tests.
- The new reader test proves canonical replay of an opening seed plus durable
  Command/Event/Inventory/Financial facts and rejects a forged Event
  fingerprint before state is returned.

## Still required before Gate B

- B's scoped review of `0021fe0a` / `00649d3` found one Major defect: a
  durable Command may validly carry `expectedWorldVersion = null`, but the
  reader coerces that value to an empty string and rejects its otherwise valid
  Event lineage. The repair must derive the predecessor version from durable
  Event evidence and prove the null case with a focused successful replay test,
  then receive a fresh scoped review.
- compose this reader into the narrow automatic delivery preparation source,
  including server-owned event/posting/outbox identity allocation and an active
  writer-lease assertion;
- real PostgreSQL multi-process and process-kill recovery evidence;
- browser E2E evidence and an owner-provided exact non-production staging
  contract for RLS/grants validation; and
- the fresh scoped V10.6 review, then the full Gate B evidence matrix.

This is local candidate code only. It authorizes neither Gate B, staging, main
merge, external deployment, nor production/shared-Supabase mutation.

## Repair and combined scoped review

`86d2bae363aca7a6e8c741eebfc239ece98e4d9f` derives the predecessor version
from the durable Event boundary, rejects a non-positive Event after-version or
a mismatch with a non-null Command expectation, and adds the null-expectation
successful replay regression. The worker typecheck and durable-reader focused
test passed locally.

B reviewed the immutable combined range
`00649d3cbca1c572b9421527199fe7a5c261e500..6c1330b7868027fa4b3b842133c599b50fbfca10`,
including the V10.7 durable delivery preparation and declaration-build repair:
`BLOCKER=0`, `MAJOR=0`, `MINOR=0`. It is approved only to prepare Gate B
evidence. The remaining real PostgreSQL, browser, and exact non-production
staging evidence is not covered by this scoped review and Gate B remains
pending.

## Local Gate B preflight

On `1a1cf3df319e0ecc9b662b33fd758b79058d2ebd`, `pnpm check` passed locally:
lint, formatting, typechecks and builds; 73 test files / 688 tests passed;
the boundary suite passed 34/34; migration validation plus clean- and
existing-schema rehearsal passed for all 16 migrations; foundation policy and
repository-secret checks passed. The runner was limited to two workers so
isolated PGlite tests do not contend for local resources; no assertion, test
budget, or timeout was relaxed. Two test files / eight tests remain
environment-conditional skips, so this local preflight is not a substitute for
the required real PostgreSQL, browser, or non-production staging evidence.
