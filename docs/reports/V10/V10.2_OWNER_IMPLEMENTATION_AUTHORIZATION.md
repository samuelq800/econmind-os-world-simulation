# V10.2 Owner Implementation Authorization

## Immutable binding

| Item                           | Value                                                                            |
| ------------------------------ | -------------------------------------------------------------------------------- |
| Owner continuation instruction | `继续` — 2026-09-14                                                              |
| Authorized branch              | `codex/v10-1-implementation`                                                     |
| ADR rule                       | `docs/architecture/decisions/ADR-09.md`                                          |
| Core implementation candidate  | `b6d3842b0cc8bd8f16756946415135fc3794355e`                                       |
| Authorized slice               | V10.2 narrow GRAIN / Buyer-Treasury-GCU proposal, approval, and reservation core |
| Required exit                  | `V10.2_INDEPENDENT_REVIEW`                                                       |

The owner authorized construction of the exact ADR-09 V10.2 slice after the
V10.1 B1 follow-up closure. This authorizes an implementation candidate only:
it does not declare V10.1 complete, does not verify V10.2, and does not alter
the independent-review requirement for either package.

The bound candidate rejects terms outside the exact registered GRAIN/GCU
fixture, binds the signed asset source and expiry to the Command fingerprint,
requires separately current Seller Trade, Buyer Trade, and Buyer Finance
signatures, and creates only an `AVAILABLE -> RESERVED` inventory posting
against an authoritative inventory state. Its focused local check passed
four scenarios: valid reservation, exact duplicate, stale approval revision,
and insufficient stock.

This authorization permits no `main` merge, deployment, migration publication,
production or shared-Supabase access/mutation, original EconMind website
change, browser authority, V10.3 delivery/payment work, or V11 work. Durable
server persistence and atomic-worker composition remain a follow-on candidate
inside the declared V10.2 boundary and require their own exact review target.
