# Independent review for STEP_ID

## Review identity and scope

- Reviewed branch and commit:
- Reviewer independent from implementation:
- Requirements and ADRs reviewed:
- Files and evidence inspected:

## Findings

For each finding record severity, location, reproduction, expected behavior,
actual behavior, and required correction.

## Validation evidence

Record every command and exit code. Classify evidence only as `PASS`, `FAIL`,
`NOT_RUN`, or `INSUFFICIENT_EVIDENCE`. A test file or implementation claim is
not proof that a test ran.

## Gate decision

Choose one: `VERIFIED`, `CHANGES_REQUIRED`, or `BLOCKED`. State which hard
requirements support the decision. A reviewer may not approve a human-required
ADR or silently defer original scope.

## Next action

If blockers exist, request blocker-only fixes and re-review. If verified, update
the controlled status record through the responsible approval process.
